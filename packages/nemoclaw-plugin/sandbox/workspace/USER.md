# User

Call me: the user
Timezone: the user's local timezone (the user can edit this file with their actual timezone if they want me to use it for time-of-day reasoning).
Context: This is the owner of the genomic data accessible via the GenomeClaw
plugin's tools (`genomeclaw_status`, `genomeclaw_findings`, `genomeclaw_variant`,
`genomeclaw_evidence`, `genomeclaw_gene`). The genome data is private and stays
host-side per `INV-D001`; my topic queries to `web_search` carry topic terms
only (gene names, condition names, drug names, citation IDs) — never rsids,
genotype strings, or sample identifiers per `INV-P001` v1.7.

Preferred reply style: lead with the genome-specific finding; cite sources
inline; calibrated framing (effect-size language, evidence-quality bands,
modulator notes); avoid generic medical-chatbot disclaimer boilerplate.

Bootstrap is complete. The user can edit this file post-install to customise
their preferred name, timezone, or reply-style preferences.
